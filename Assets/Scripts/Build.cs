using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using gatherMech;


public class Build : MonoBehaviour
{
    // Class yapýsý kullanmayý denedim, yapamadým. Gathering.cs dosyasýndan odun sayýsýna eriþip buradan deðerini azaltmayý denedim.

    /*private int buildingCount = 0;
    [SerializeField] GameObject _temel;
    [SerializeField] GameObject _orta;
    [SerializeField] GameObject _son;
    [SerializeField] GameObject _alan;

    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag("OkcuBina"))
        {
            buildingCount++;
            if (buildingCount <= 1 && odun >= 2)
            {
                odun= odun-2;
                _temel.gameObject.SetActive(true);
            }
            if (buildingCount > 1 && buildingCount <= 2)
            {
                _orta.gameObject.SetActive(true);
            }
            if (buildingCount > 2 && buildingCount <=3)
            {
                _son.gameObject.SetActive(true);
                _alan.gameObject.SetActive(false);
            }
        }
    }*/
}
